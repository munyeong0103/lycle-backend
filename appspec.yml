version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/lycle-backend
    overwrite: yes
permissions:
  - object: /
    pattern: "**"
    owner: ec2-user
    group: ec2-user
hooks:
  AfterInstall:
    - location: deploy.sh
      timeout: 200
      runas: ec2-user
